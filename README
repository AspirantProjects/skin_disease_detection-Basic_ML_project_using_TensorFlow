# skin disease dectection - Basic ML project using TensorFlow

Steps to setup the project on the machine:

Python 3.8v (must)
python -m pip install keras (or) pip install keras
python -m pip install flask
python -m pip install gevent
python -m pip install pandas
python -m pip install scikit-learn scipy matplotlib
python -m pip install flask-security
python -m pip install email-validator
python -m pip install sqlalchemy
python -m pip install opencv-python
python -m pip install numpy
python -m pip install tensorflow (if required)
python -m pip install tensorflow-gpu (if required)

To execute the output --> run (app.py)
It will run an server on http://localhost:5000/ portal
If server runs fine, then enter email as: test@example.com and password as : password 
Now you can able to test skin disease 

Note: If you found import Error : DLL not found (kindly use py 3.8v)
